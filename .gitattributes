# Normalize line endings to LF in the repo (Foundry often runs on Linux hosts).
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.gif binary
*.ldb binary
*.log binary
